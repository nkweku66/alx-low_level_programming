#include "main.h"

/**
 * _abs - returns the absolute a number
 * @n: argument
 *
 * Return: n
 * Author - Nana Obeng
 */

int _abs(int n)
{
	if (n < 0)
	{
		n *= -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
