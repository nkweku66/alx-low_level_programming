#include "main.h"

/**
 * main -checks to see if a character is uppercase
 * @c -character to be checked
 * Return 0 indicating completion
 */

int _isupper(int c)
{
	return ( c >= 'A' && c <= 'Z');
}
