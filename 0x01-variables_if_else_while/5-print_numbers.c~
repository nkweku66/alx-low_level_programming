#include <stdio.h>

/**
 * main - entry of program
 *
 * Return: 0 on success
 * Author - Nana Obeng
 */

int main(void)
{
	int a = 0;

	for (; a <= 10; a++)
	{
		printf("%d", a);
	}
	putchar('\n');
	return (0);
}
